# 이 저장소에서 일하는 방식

Claude Code가 이 저장소에서 작업할 때 지킬 것들입니다.
**컴퓨터가 바뀌어도 같은 규칙이 적용되게** 하려고 저장소 안에 둡니다.

프로젝트가 무엇이고 어떻게 실행하는지는 [README.md](README.md),
왜 그렇게 설계했는지는 [docs/backend-design.md](docs/backend-design.md)를 보세요.

---

## 개발하면 노션도 같이 갱신한다

**이 프로젝트의 산출물은 코드만이 아니라 포트폴리오입니다.** 나중에 몰아서 쓰면
"왜 그렇게 했는지"가 날아가고, 특히 시행착오가 제일 먼저 잊힙니다.
그래서 **코드를 커밋·push한 뒤 같은 턴에서 노션까지 갱신하고 끝냅니다.**
노션 업데이트를 별도 요청으로 미루지 않습니다.

갱신하는 곳은 두 군데입니다.

### 1. 「김병오」 페이지 — 날짜별 작업 일지

`3c3cac82-0c6d-801d-8634-e7a1e0265a78`

하루치를 이 형식으로 씁니다.

```
# 2026-MM-DD
담당: 웹 전체 (프론트 + 백엔드 + DB)
<callout> 오늘이 어떤 날이었는지 두세 줄 + 결과 링크 </callout>

**굵은 소제목**
- 불릿
```

**반드시 들어가야 하는 것 — 오늘 무슨 문제가 있었고, 어떻게 해결했고, 지금 어떤 상태인가.**
막힌 것은 이 순서로 적습니다.

1. **증상** — 무엇이 어떻게 잘못 보였나
2. **원인** — 진짜 원인. 표면 증상 말고
3. **왜 여기서만 나타났나** — 로컬/배포, 특정 역할, 특정 화면처럼 조건이 있었다면
4. **해결** — 어떤 순서로 좁혀 들어가 무엇을 고쳤나
5. **배운 것** — 다음에 같은 실수를 안 하려면

마지막에 `📍 지금 상태` callout으로 "무엇이 되고, 남은 건 무엇이며, 그중 무엇이
내 몫이 아니라 기다리는 것인지"를 정리합니다.

### 2. 「포트폴리오 제작」 페이지 — 주제별 문서

`3cacac82-0c6d-8050-9cda-fae1ad24fd21`

- 맨 앞 `0. 날짜별 진행` 표에 한 줄 추가
- 내용이 실제로 바뀐 장을 같이 고칩니다 (권한 표, "서버가 막는 것" 표, 검증 건수, 남은 것 등)
- **하루 단위 상세는 여기 쓰지 않습니다.** 이 문서는 주제별 구성이라 날짜를 섞으면
  읽기 어려워집니다 — 날짜 축은 표로만 두고 상세는 「김병오」 페이지에 씁니다

### 자주 쓰는 노션 페이지 ID

검색으로 찾으면 동명 페이지를 집을 수 있으니 ID로 바로 접근합니다.

| 페이지 | ID |
| --- | --- |
| 김병오 (날짜별 일지) | `3c3cac82-0c6d-801d-8634-e7a1e0265a78` |
| 포트폴리오 제작 | `3cacac82-0c6d-8050-9cda-fae1ad24fd21` |
| 로봇안전AI (팀 루트) | `6b7cac82-0c6d-83b7-80b1-81e9b9626fd7` |
| 브랜드 · 로고 | `3cccac82-0c6d-8111-8d6e-fc65030a633d` |

> 노션 API에 한글을 보낼 때 `\uXXXX` 이스케이프를 손으로 만들면 글자가 깨집니다
> (`쪼개`가 `쯪개`가 되는 식). 한글은 그대로 보내고, 고칠 때는 `update_content`의
> `old_str`이 정확히 일치하는지 먼저 `fetch`로 확인하세요.

---

## 코드에서 지킬 것

- **권한 함수는 `center-web/lib/types.ts` 한 곳에만.** 화면마다 흩어놓지 않습니다
- **화면 검사는 UX이고 실제 차단은 서버(Route Handler)가 합니다.** 둘 다 넣습니다
- **역할은 ID 토큰의 커스텀 클레임에서만 읽습니다.** 요청 본문의 역할은 믿지 않습니다
- **역할을 바꾸면 Firestore와 커스텀 클레임을 같이 갱신**하고 리프레시 토큰을 폐기합니다
- **색·모서리·여백은 `center-web/app/tokens/center.css`에서** 고칩니다.
  개별 화면 CSS를 돌아다니며 고치면 화면마다 다른 값이 생깁니다
- **경계선은 `border`가 아니라 `inset box-shadow`.** 상태가 바뀌어도 레이아웃이 안 밀립니다
- **파생값은 저장하지 않습니다.** 자격 유효/만료는 `expiresOn`에서, 연차는 `hiredOn`에서 계산
- **지우는 대신 무효화합니다.** 계정은 `active: false`, 사원증은 `revokedAt` —
  지우면 과거 이력의 이름이 빈칸이 됩니다
- **얼굴 사진·특징값은 웹에 저장하지 않습니다.** 젯슨이 판정하고 웹은 등록 여부만 압니다

## 배포 전에

`main`에 push하면 Vercel이 자동 배포합니다. 그래서 push 전에:

```bash
cd center-web
npx tsc --noEmit && npx eslint .
npm run build          # 로컬 dev에서는 안 잡히는 번들링 문제가 여기서 잡힙니다
```

`next dev`는 서버 코드를 번들링 없이 돌리기 때문에, **배포에서만 터지는 문제가
로컬에서 끝까지 안 보일 수 있습니다.** 실제로 `jose@6`의 CJS 빌드 누락으로
`/api/me`가 배포에서만 500이 난 적이 있습니다 (자세한 건 포트폴리오 8장).
